clc;
clear all;

syms t
pd = [0.42*cos(t *pi/10); 0.42*sin(t*pi/10); 0.1*(1+sin(t))];
pd_dot = diff(pd)
sym2poly(t)
t = 1:.1:2;
q1=zeros(length(pd),length(pd));
q2=zeros(length(t),length(pd_dot));
for t = 1:.1:2;
   
        q1(t,:) = pd(t);
        q2(t,:) = pd_dot(t);
   
end
q1
q2
    